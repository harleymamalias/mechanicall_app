import 'package:flutter/material.dart';

class VehicleOwnerBookingAppointmentDetailsPage extends StatelessWidget {
  const VehicleOwnerBookingAppointmentDetailsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const SafeArea(
      child: Scaffold(
        body: Column(
          children: [
            Text('Appointment details'),
          ],
        ),
      ),
    );
  }
}